# All landfill timelapses, in one place

Convenience copies of every `.gif` produced by every iteration. Each file
is also a duplicate of the corresponding source in
`../iterations/<NAME>/timelapse_*.gif`.

| file                                | what                                    |
|-------------------------------------|-----------------------------------------|
| `01-midpoint-2km__truecolor.gif`    | 2 km AOI midpoint, both dumps, 14 frames |
| `01-midpoint-2km__ndvi.gif`         | same dates, NDVI palette                |
| `02-muni-bsi__truecolor.gif`        | 800 m AOI muni-only, 35 frames          |
| `02-muni-bsi__ndvi.gif`             | same dates, NDVI                        |
| `02-muni-bsi__bsi.gif`              | same dates, BSI (water-aware)           |

Findings that go with these are in `../REPORT.md`.
The headline chart is `../iterations/02-muni-bsi/waste_area_chart.png`.
